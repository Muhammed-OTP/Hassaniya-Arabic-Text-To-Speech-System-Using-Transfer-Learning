# Core data & numerics
numpy>=1.24.0,<3.0.0
pandas>=2.0.0
pyarrow>=12.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# Deep learning (keep torch + torchaudio versions aligned)
torch>=2.0.0
torchaudio>=2.0.0

# TTS demo
gTTS>=2.3.0

# Text utilities
arabic-reshaper>=3.0.0
python-bidi>=0.4.2
regex>=2023.0
tqdm>=4.65.0
scikit-learn>=1.3.0

# Notebooks (lighter than meta-package "jupyter" — avoids huge dependency tree)
ipykernel>=6.29.0
ipython>=8.0.0
notebook>=7.0.0
